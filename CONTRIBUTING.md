# BASE11 기여 및 브랜치 운영 규칙

## 기본 원칙

- `main`은 항상 배포 가능한 상태로 유지합니다.
- `main`에 직접 푸시하지 않고 작업 브랜치에서 Pull Request를 만듭니다.
- 한 브랜치는 한 가지 목적만 담고, 리뷰 가능한 크기로 유지합니다.
- 병합 후 원격과 로컬 작업 브랜치는 삭제합니다.

## 브랜치 이름

`<type>/<short-description>` 형식을 사용합니다. 설명은 영문 소문자와 하이픈으로 작성합니다.

| Type | 용도 | 예시 |
| --- | --- | --- |
| `feat` | 사용자 기능 | `feat/session-publication` |
| `fix` | 버그 수정 | `fix/duplicate-attendance` |
| `refactor` | 동작을 유지하는 구조 개선 | `refactor/coaching-repository` |
| `docs` | 문서만 변경 | `docs/local-setup` |
| `chore` | 도구, 의존성, 저장소 관리 | `chore/ci-workflow` |
| `codex` | Codex가 수행하는 작업 | `codex/elite-coaching-platform` |

긴 작업은 기능별 브랜치로 나눕니다. 브랜치 이름에 개인 이름이나 날짜는 넣지 않습니다.

## 커밋

- 커밋 제목은 명령형 현재 시제로 간결하게 작성합니다.
- API, 웹, 모바일처럼 독립적으로 검토 가능한 변경은 별도 커밋으로 나눕니다.
- 생성 파일은 원본 변경과 같은 커밋에 포함합니다.
- 비밀값, 개인 설정, 로컬 캐시와 빌드 결과는 커밋하지 않습니다.

## Pull Request

1. 최신 `main`을 반영하고 충돌을 해결합니다.
2. `make check`를 실행합니다. 일부 영역만 변경했다면 관련 검사와 생략 이유를 PR에 기록합니다.
3. 변경 목적, 사용자 영향, 검증 결과를 PR 템플릿에 작성합니다.
4. 리뷰가 끝나면 **Squash and merge**로 병합합니다.
5. 병합 후 브랜치를 삭제합니다.

긴급 수정도 `fix/*` 브랜치와 PR을 사용합니다. 서비스 복구 때문에 선병합이 필요했다면 즉시 후속 PR과 원인 기록을 남깁니다.

## GitHub 설정 기준

저장소 요금제에서 브랜치 보호가 지원되면 `main`에 다음 규칙을 적용합니다.

- Pull Request 없이 변경 금지
- 병합 전 승인 1명 이상
- 새 커밋이 올라오면 기존 승인 무효화
- 필수 상태 검사 통과
- 대화가 해결된 경우에만 병합
- 강제 푸시와 브랜치 삭제 금지
- 병합 후 작업 브랜치 자동 삭제
