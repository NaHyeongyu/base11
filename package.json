{
  "name": "clubhaus-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @clubhaus/web",
    "build:web": "npm run build --workspace @clubhaus/web",
    "lint:web": "npm run lint --workspace @clubhaus/web",
    "typecheck:web": "npm run typecheck --workspace @clubhaus/web"
  },
  "engines": {
    "node": ">=20.19.0 <21"
  }
}

